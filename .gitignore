# macOS
.DS_Store

# Build output
runtime/PolaroidsSaver/build/
dist/
*.saver/

# Python pipeline
pipeline/.venv/
pipeline/.pytest_cache/
pipeline/__pycache__/
pipeline/**/__pycache__/
pipeline/*.egg-info/
pipeline/state.sqlite*
pipeline/pool/

# Personal photo pool — never commit these
~/Pictures/Polaroids for Mom/
applephotos/
Drive/
facebook/
Instagram/
runtime/web/photos/

# Generated photo manifest (regenerated by scripts/refresh-photos.sh from
# whatever lives in ~/Pictures/Polaroids for Mom/). Kept out of git so
# personal photos never end up in the repo history.
# The committed photos-data.js ships empty; uncomment to also ignore local edits:
# runtime/web/js/photos-data.js

# Editor / IDE
.vscode/
.idea/
*.swp
